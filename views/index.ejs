<%- include('_header.ejs') -%>
<section class="col-xs-12">

  <div class="panel panel-primary">
    <div class="panel-heading">
      <h2>Welcome <%= username %></h2>
    </div>
  </div>
    
  <table class="table table-dark">
    <thead>
      <tr>
        <th scope="col">NO</th>
        <th scope="col">TITLE</th>
        <th scope="col">DESCRIPTION</th>
        <th scope="col">AUTHOR</th>
        <th scope="col">EMAIL</th>
        <th scope="col">EDIT</th>
      </tr>
    </thead>
    <tbody>

  GaryKim  11:11 AM
  <% for(i=0;i<data.length;i++) { %>
     <tr>
       <th scope="row"><%= i+1 %></th>
       <td><%= data[i].title %></td>
       <td><%= data[i].description %></td>
       <td><%= data[i].email %></td>
       <td><%= data[i].author %></td>
       <td><%= data[i].createAt %></td>
       <td>
         <from><a href="/delete/<%= data[i]._id %>" onsubmit="return confirm('정말 삭제 하시겠습니까?')"><button type="button" class="btn btn-danger">삭제</button></a></from>
         <from><a href="/delete/<%= data[i]._id %>" onsubmit="return confirm('정말 삭제 하시겠습니까?')"><button type="button" class="btn btn-danger">수정</button></a></from>
        </td>
     </tr>
     <% } %>
    </tbody>
  </table>
  <div style="justify-content: end;">
    <a href="/content"><button type="button" class="btn btn-primary " >CREATE</button></a>
</div>
  </section>
    <%- include('_footer.ejs') -%>